# ☕ Sistema Bancário em Java

Mini-projeto desenvolvido em **Java puro**, com base na **Programação Orientada a Objetos (POO)**.  
O objetivo é simular um sistema bancário simples com operações de saque, depósito e consulta de saldo.

---

## 🧩 Funcionalidades

**Métodos principais:**
- **depositar(double valor)** → adiciona um valor ao saldo. Retorna `boolean`.
- **sacar(double valor)** → realiza o saque caso o saldo seja suficiente. Retorna `boolean`.
- **getTitular()** → retorna o nome do titular.
- **getCpf()** → retorna o CPF do titular.
- **getSaldo()** → retorna o saldo atual.

---

## 🧱 Estrutura de Pastas

```bash
SistemaBancario/
├── .gitignore
├── README.md
└── src/
    └── main/java/br/victorpolicarpo/sistemabancario/
        ├── dominio/
        │   └── Conta.java
        └── Main.java
```
## ⚙️ Tecnologias Utilizadas

- Java 17  
- IntelliJ IDEA  
- Git & GitHub  

